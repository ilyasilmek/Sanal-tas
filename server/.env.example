PORT=8080
# Must match AntiCheat.DEFAULT_HMAC_SECRET in the Android client (see PR notes:
# a client-embedded secret is not real protection against a determined attacker).
HMAC_SECRET=super_secret_rock_key_2026
